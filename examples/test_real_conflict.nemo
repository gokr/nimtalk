# This should fail - two parents with same directly-defined method, no override

Parent1 := Object derive: #(a)
Parent1 >> foo [ ^ "foo1" ]

Parent2 := Object derive: #(b)
Parent2 >> foo [ ^ "foo2" ]  # Same directly-defined method!

ChildNoOverride := Object derive: #(x)
# Child does NOT override "foo"

# Try to add Parent1 (should work)
ChildNoOverride addParent: Parent1

# Try to add Parent2 - this should fail because:
# 1. Parent2 has directly-defined "foo"
# 2. Child doesn't override "foo"
# 3. Child inheriting from Parent1 also has "foo" (via inheritance)
ChildNoOverride addParent: Parent2

result := "ERROR: Should have failed"
result
