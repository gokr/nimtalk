#!/usr/bin/env harding
# Test recursion

Counter := Object derive.

Counter>>countDown: n [
  n = 0 ifTrue: [
    ^ 0
  ] ifFalse: [
    ^ self countDown: (n - 1)
  ]
]

c := Counter new.
result := c countDown: 5.
result
