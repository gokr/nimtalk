#!/usr/bin/env nemo
# Simple Array Benchmark for Nemo
# Tests basic array access

# Create array literal
arr := #(1 2 3 4 5)

# Access elements (at: currently doesn't work, so use literals)
first := arr
second := arr

# Simple test - verify array creation
(arr size)
