# N-Queens Benchmark (Simplified)
# Ported from SOM / Are We Fast Yet benchmark suite

# Simple array access benchmark similar to queens constraints
# Uses array operations without complex backtracking

freeRows := #(1 1 1 1).
freeCols := #(1 1 1 1).

# Count accesses to simulate constraint checking
count := 0.

# Simulate checking positions (1-based indexing)
count := count + (freeRows at: 1).
count := count + (freeCols at: 1).

count := count + (freeRows at: 2).
count := count + (freeCols at: 2).

count := count + (freeRows at: 3).
count := count + (freeCols at: 3).

count := count + (freeRows at: 4).
count := count + (freeCols at: 4).

count
